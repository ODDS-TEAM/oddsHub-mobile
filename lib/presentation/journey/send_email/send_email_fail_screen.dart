import 'package:flutter/material.dart';

class SendMailFailScreen extends StatelessWidget {
  const SendMailFailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Fail Send Mail');
  }
}