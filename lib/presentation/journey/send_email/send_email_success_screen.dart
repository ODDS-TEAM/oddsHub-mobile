import 'package:flutter/material.dart';

class SendMailSuccessScreen extends StatelessWidget {
  const SendMailSuccessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Success Send Mail');
  }
}